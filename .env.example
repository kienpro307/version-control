# Supabase Configuration
# Get these from: https://supabase.com/dashboard/project/YOUR_PROJECT/settings/api

# Your Supabase Project URL (e.g., https://xxxxx.supabase.co)
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co

# Supabase Anonymous Key (public, safe to expose in frontend)
# Find in: Supabase Dashboard → Settings → API → Project API keys → anon public
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-public-key-here

# API Secret Key for MVM REST API authentication
# Generate using: npm run generate-api-key
# Or manually: openssl rand -base64 32 | tr -d '\n' | sed 's/^/mvm_sk_live_/'
API_SECRET_KEY=mvm_sk_live_your-generated-secret-key-here

# Optional: Local development settings
# NODE_ENV=development
# PORT=3000
